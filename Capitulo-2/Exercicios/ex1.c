#include <stdio.h>
//Idetifique e corrija os erros em cada uma das instruções a seguir:

int main() {
    //2.7
    //A
    int valor, x,y,z;
    scanf("%d", &valor);

    //B
    printf("O produto de %d e %d e %d sao: ", x,y,z);

    //C
    valor = x + y + z;

    //D
    if(x >= y){};

    //E
    /*Programa pra determinar o maior de 3 inteiros*/

    //F
    scanf("%d", y);

    //G
    printf("O resto de %d dividido por %d e %d", x,y,z);

    //H
    if(y == z){};

    //I
    printf("%d + %d = %d", x,y, (x + y));

    //J
    printf("O vlaor forcenido e %d\n", num);



}

